# 更新日志

## 2026-08-28

### 精简内置分类
- 移除圆形（circle）和矢量（svg）两个较少使用的内置分类
- 图标总数精简至 1390

### 上传与移动支持所有分类
- 上传图标时可选择所有分类（内置 + 自定义），不再限于自定义分类
- 移动图标时同样支持选择内置分类作为目标

### 分类重命名增强
- 所有分类（包括内置分类）均支持重命名
- 内置分类重命名修改显示标签，实际目录不变

### 新增图标分类
- 新增 5 个内置分类：AI（64）、云服务（61）、Docker（101）、NAS（35）、PT（46）

### 批量操作 & 分类管理增强
- 新增多选模式，支持批量移动、批量删除图标
- 去掉图标卡片左上角的分类标签，界面更简洁

### 图标管理功能
- 支持对所有图标（包括内置图标）执行重命名、移动、删除操作
- 支持在 WebUI 创建、重命名和删除自定义分类
- 上传图标时可选择目标分类
- 分类标签栏动态渲染，显示各分类图标数量

## 2026-08-27

### 双存储后端
- 支持 Workers KV 和 R2 两种存储方式，部署者二选一
- Workers KV 无需绑定信用卡，降低部署门槛
- 统一存储抽象层，自动检测绑定类型

### Web 页面鉴权
- 首次访问可直接在网页设置管理员账号
- 用户名密码存储在 R2/KV 中，无需配置环境变量
- 支持登录、登出、修改密码
- PBKDF2-SHA256 密码哈希，JWT Cookie 鉴权
- 未设置账号时上传/删除对所有人开放（向后兼容）

### 单仓库架构
- 将 1754+ 张高清图标合并到项目仓库中
- 移除对外部 HD-Icons 仓库的依赖
- Fork 一个仓库即可完成部署，无需修改代码
- 移除同步功能（图标已内置）

### 项目初始化
- 基于 xushier/HD-Icons 创建 Cloudflare Pages 部署版本
- Cloudflare Pages + Pages Functions + R2 架构
- 搜索、筛选、复制地址、放大预览
- 自定义图标上传/删除
- 日间/夜间模式切换
- MIT License
